# Metalúrgica Alfa - Frontend (Next.js minimal)

This is a minimal Next.js frontend prepared for static export (next export).

Quick start:
1. npm install
2. npm run build
3. npm run export
4. Serve the generated out/ directory or deploy to Render / Vercel / Lovable.

API endpoints are mocked under pages/api for demo. Replace with real backend URL in production.
